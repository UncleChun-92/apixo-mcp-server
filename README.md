# apixo-mcp-server

APiXO local MCP server project (V1).

## Purpose

This repository will host the MCP server package published as `@apixo/mcp-server`.

## Current Status

- Repository initialized
- Basic Node/TypeScript scaffold created
- Ready for implementation and npm publish workflow

## Planned Tools (V1)

- `apixo_generate_task`
- `apixo_get_task_status`
- `apixo_get_balance`

## Next Steps

1. Implement MCP stdio server entrypoint.
2. Implement APiXO API client and auth handling.
3. Add integration tests for core tools.
4. Publish package to npm as `@apixo/mcp-server`.

